package com.constancio.domain.model

/**
 * Represent a single Next Path Response.
 *
 * @property nextPath The next path to fetch the response code.
 */
data class NextPath(
    val nextPath: String
)